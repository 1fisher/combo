name: CI / Release

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      version_type:
        description: 版本类型
        required: true
        type: choice
        default: patch
        options: [patch, minor, major]
      prerelease:
        description: 标记为预发布
        required: false
        type: boolean
        default: false
      tag:
        description: 已有 tag(跳过版本 bump 直接构建该 tag;bump 流程结束时自动传入)
        required: false
        type: string
        default: ''

env:
  CARGO_TERM_COLOR: always

# 最小权限:默认只读 contents(ci 两个 job 仅需读代码/跑检查);
# bump-version / build / publish-release 在各自 job 内显式提升为 contents: write。
permissions:
  contents: read

jobs:
  # ─── CI: 前端 + 后端检查(push main / PR,跳过 tag) ───────────────────
  ci-frontend:
    name: CI Frontend
    # 常规 push/PR 才跑 CI;workflow_dispatch 要么刚跑过 PR 合并的 CI(bump 路径)、
    # 要么是纯构建(tag 路径),都不重复
    if: "github.event_name == 'push' || github.event_name == 'pull_request'"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run tsc
      - run: npm test
      - run: npm run build

  ci-backend:
    name: CI Backend
    if: "github.event_name == 'push' || github.event_name == 'pull_request'"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            crates/combo-cli
            src-tauri
      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libgtk-3-dev librsvg2-dev \
            patchelf libsoup-3.0-dev libjavascriptcoregtk-4.1-dev \
            libayatana-appindicator3-dev

      # tauri-build 的 build script 会在编译期校验 bundle.resources 里的 ../dist
      # (src-tauri/tauri.conf.json 的 {"../dist/": "dist/"})——不存在即报
      # "resource path `../dist` doesn't exist"。因此必须先构建前端产物。
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Build frontend (dist/ required by tauri-build)
        run: |
          npm ci
          npm run build

      - run: cargo check --workspace --verbose
      - run: cargo test --workspace --verbose

  # ─── 版本 bump + tag(workflow_dispatch 专用) ──────────────────────────
  bump-version:
    name: Bump Version
    # 带 tag 的 dispatch 是「按 tag 构建」路径,直接进 build,不再重复 bump
    if: github.event_name == 'workflow_dispatch' && inputs.tag == ''
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      actions: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      - name: Bump version
        id: bump
        run: |
          bash scripts/version.sh ${{ inputs.version_type }}
          NEW_VERSION=$(node -e "console.log(require('./package.json').version)")
          echo "version=${NEW_VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=v${NEW_VERSION}" >> "$GITHUB_OUTPUT"
      - name: Generate changelog
        id: changelog
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || echo "")
          RANGE="${PREV_TAG:+$PREV_TAG..HEAD}"
          RANGE="${RANGE:-HEAD}"
          {
            echo "content<<CHANGELOG_EOF"
            echo "## 变更内容"
            echo ""
            git log "${RANGE}" --pretty=format:"- %s (%h)" --no-merges
            echo ""
            echo ""
            if [ -n "$PREV_TAG" ]; then
              echo "**完整变更**: https://github.com/${{ github.repository }}/compare/${PREV_TAG}...${{ steps.bump.outputs.tag }}"
            fi
            echo "CHANGELOG_EOF"
          } >> "$GITHUB_OUTPUT"
      # main 开启分支保护(PR-only,管理员同样强制)后 bot 无法直推 main:
      # 版本 bump 改走「bump 分支 → PR → 自动合并 → 在合并后的 main 提交上打 tag」。
      # tag 推送不受分支保护限制,并触发下方 build(仅 tag 触发)完成多平台构建。
      - name: Commit and merge via PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.bump.outputs.version }}"
          BRANCH="chore/bump-v${VERSION}"
          git checkout -b "$BRANCH"
          git add -A
          git commit -m "chore: bump version to ${VERSION}"
          # --force:上次 run 失败可能残留同名分支(如 pr create 权限问题),
          # bump 分支为一次性 bot 分支,强推安全
          git push --force origin "$BRANCH"
          gh pr create --head "$BRANCH" \
            --title "chore: bump version to ${VERSION}" \
            --body "版本发布自动 bump(release 工作流)"
          gh pr merge "$BRANCH" --merge --delete-branch

      - name: Tag merged main, then trigger tag build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.bump.outputs.tag }}"
          git fetch origin main
          git tag -a "$TAG" origin/main -m "Release $TAG" -m "${{ steps.changelog.outputs.content }}"
          git push origin "$TAG"
          # GITHUB_TOKEN 发起的 push 不会触发下游 workflow(GitHub 防递归机制,
          # 此前 v0.2.20 tag 推了却没有任何构建就是这个原因),这里显式 dispatch
          # 一次并携带 tag,走下方 build 的「按 tag 构建」分支
          gh workflow run release.yml --ref main -f version_type=patch -f tag="$TAG"

  # ─── 多平台构建(tag push,或 bump 后携带 tag 自触发) ────────────────
  build:
    name: Build (${{ matrix.platform }})
    # 仅按 tag 构建(tag push,或 bump 结束后携带 tag 的自 dispatch)。
    # 不能在无 tag 的 dispatch 上构建:github.ref_name 是 main,
    # tauri-action 会把产物挂到错误的「Combo main」release
    if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.tag != '')
    permissions:
      contents: write
    env:
      HAS_APPLE_SIGNING: ${{ secrets.APPLE_CERTIFICATE != '' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-aarch64
            runner: macos-14
            target: aarch64-apple-darwin
          - platform: macos-x86_64
            runner: macos-15
            target: x86_64-apple-darwin
          # Windows 暂不支持:combo-cli 依赖 Unix 风格路径与符号链接
          # - platform: windows-x86_64
          #   runner: windows-latest
          #   target: x86_64-pc-windows-msvc
          - platform: linux-x86_64
            runner: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.tag || github.ref_name }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            crates/combo-cli
            src-tauri

      - name: Install dependencies (Linux only)
        if: matrix.platform == 'linux-x86_64'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libgtk-3-dev librsvg2-dev \
            patchelf libsoup-3.0-dev libjavascriptcoregtk-4.1-dev \
            libayatana-appindicator3-dev
          # tauri-cli 打包阶段会检测 appindicator(系统托盘依赖),缺失即 panic:
          # "Can't detect any appindicator library"(v0.2.19 Linux 构建失败根因)

      - name: Install frontend dependencies
        run: npm ci

      # macOS 代码签名 + 公证(可选):在 GitHub Secrets 配置以下 6 项后自动生效,
      # 构建即为「Developer ID 签名 + 已公证」,下载可正常双击打开,且 TCC 隐私
      # 权限(麦克风/文件夹等)按稳定签名身份记录,系统设置里能正常列出 Combo。
      # 未配置时本步跳过、env 不注入,tauri-cli 回退为 ad-hoc 签名
      # (tauri.conf.json 的 signingIdentity "-"),首次打开需右键 → 打开。
      # 注意:必须用 if 条件守卫,不能直接 env: ${{ secrets.X }} —— 未配置的
      # secret 会变成空字符串环境变量,tauri-cli 视其为「已设置」,导入空证书
      # 直接构建失败(此前 c85d514 回退的原因)。
      - name: Export Apple signing secrets
        if: env.HAS_APPLE_SIGNING == 'true'
        run: |
          echo "APPLE_CERTIFICATE=${{ secrets.APPLE_CERTIFICATE }}" >> "$GITHUB_ENV"
          echo "APPLE_CERTIFICATE_PASSWORD=${{ secrets.APPLE_CERTIFICATE_PASSWORD }}" >> "$GITHUB_ENV"
          echo "APPLE_SIGNING_IDENTITY=${{ secrets.APPLE_SIGNING_IDENTITY }}" >> "$GITHUB_ENV"
          echo "APPLE_ID=${{ secrets.APPLE_ID }}" >> "$GITHUB_ENV"
          echo "APPLE_PASSWORD=${{ secrets.APPLE_PASSWORD }}" >> "$GITHUB_ENV"
          echo "APPLE_TEAM_ID=${{ secrets.APPLE_TEAM_ID }}" >> "$GITHUB_ENV"

      - name: Build and upload
        uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Updater 签名(用于自动更新校验)
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ inputs.tag || github.ref_name }}
          releaseName: 'Combo ${{ inputs.tag || github.ref_name }}'
          releaseBody: 'See assets below.'
          releaseDraft: true
          # build 仅由 tag push 触发,预发布标记不再从 dispatch 透传(需要预发布时
          # 在 GitHub Release 页手动勾选,或给 tag 名加 -rc.N 后缀后自行调整此条件)
          prerelease: false
          updaterJsonPreferNsis: true
          args: --target ${{ matrix.target }}

  # ─── 发布 Release(所有构建完成后) ─────────────────────────────────────
  publish-release:
    name: Publish Release
    needs: build
    if: always() && needs.build.result != 'skipped'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Publish draft release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ inputs.tag || github.ref_name }}"
          if [ "${{ needs.build.result }}" != "success" ]; then
            echo "⚠️ 部分构建失败,仍发布已上传的产物"
          fi
          # -R 必须显式指定:本 job 无 checkout,gh 无法从 git remote 推断仓库,
          # 此前该步骤一直静默失败(历史 v0.2.15/17/18 停在 Draft 就是这个原因)
          if ! gh release edit "$TAG" -R "$GITHUB_REPOSITORY" --draft=false; then
            echo "⚠️ 自动发布失败,请到 Releases 页手动 Publish $TAG"
            exit 1
          fi
          echo "✓ Release $TAG 已发布"
